__pycache__/
*.pyc
.pytest_cache/
logs/
.env
.tg_bridge_state.json
tg_convo.log
.tg_probe.session
.tg_probe.codehash
.tg_probe.session-journal
.tg_bridge_streams.json
.agents_auth_secret

# runtime config / state (copy agents.example.json -> agents.json and edit)
agents.json

# per-machine agent session ids (keep your conversations resumable)
.sessions/

# author-specific OpenOrchestra launchers (token-bearing, not part of AgentDeck)
launch-orchestra.sh
idle_reaper.log
.idle_reaper_state.json
.idle_reaper_state.json.tmp

# numbered-slot terminals (before the session library): this server still runs a few
# of them, but they are machine-specific and not part of AgentDeck any more
launch-claude*.sh
launch-claude*.sh.pre-library
launch-test.sh
test-launch-claude.sh
_order_gate.py
tests/test_order_gate.py
tests/test_launch_script_gate.py

# written by ./https.sh (host ports 80/443 for HTTPS); ./https.sh --off removes it
docker-compose.https.yml

# written by install.sh in a folder it cloned itself (lets --uninstall --purge delete it)
.agentdeck-managed
